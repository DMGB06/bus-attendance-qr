# Solo publicar mobile — lo demás queda local (/shared = solo carpeta en raíz del repo)
docs/
scripts/
/shared/
index.html
supabase/

# Datos sensibles / generados (por si acaso)
*.csv
scripts/output/

# Entorno
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/