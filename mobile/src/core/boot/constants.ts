/** Tiempo mínimo visible del splash en el primer arranque (ms). */
export const MIN_BOOT_SPLASH_MS = 1_400;

/** Tiempo mínimo visible del splash tras iniciar sesión (ms). */
export const MIN_POST_AUTH_SPLASH_MS = 700;

/** Duración del fade-out al ocultar el splash (ms). */
export const BOOT_SPLASH_FADE_MS = 380;
